classdef TS
    properties                                                              % Properties.
        period                                                              % Time period, <= 0 indicates aperiodic.
        fs                                                                  % Sampling frequency, = 1 indicates discrete-time.
    end
    
    methods (Access = private)                                              % Inline methods.

    end

    methods                                                                 % Member methods.

    end

    methods (Static)                                                        % Static methods.

    end
end
